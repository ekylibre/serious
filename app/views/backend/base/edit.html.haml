= simple_form_for [:backend, resource], class: 'form' do |f|
  = render 'form', f: f
  .btn-group
    = button_tag( :update.ta, class: 'btn btn-primary')
    = link_to( :cancel.ta, {action: :show, controller: :games}, class: 'btn btn-default')

